pipeline {
   agent any

   stages {
      stage('Hello') {
         steps {
           container('gcloud') {
									sh '''
									gcloud --version
									'''
									}
         }
      }
   }
}
