pipeline {
   agent any

   stages {
      stage('Hello') {
         steps {
           container('kubectl') {
									sh '''
									kubectl get pods
									'''
									}
         }
      }
   }
}
