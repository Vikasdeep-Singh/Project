pipeline {
   agent kubernetes

   stages {
      stage('Hello') {
         steps {
           container('kubectl') {
									sh '''
									kubectl get pods
									'''
									}
         }
      }
   }
}
