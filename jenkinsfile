pipeline {
   agent any

   stages {
      stage('Check Version of Gcloud') {
         steps {
            sh '''
	    gcloud --version
	    '''
         }
      }
      
      stage('To check number of nodes in cluster') {
         steps {
            sh '''
            kubectl get nodes
            '''
         }
      }
      
      stage('Upload new docker image to container registry repo') {
         steps {
            sh '''
            ls -lrt
            '''
         }
      }
  }
}


